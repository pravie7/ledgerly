export default function Header({ title }) {
  return (
    <div className="header">
      <div>
        <h1>{title}</h1>
        <p>Private Personal Finance Dashboard</p>
      </div>
    </div>
  );
}
